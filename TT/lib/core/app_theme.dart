// Backwards-compatible re-export.
//
// The theme now lives in `core/theme/`. Import that directly for new code
// (`import '../../../core/theme/app_theme.dart'`); this shim keeps older
// imports working during the migration.
export 'theme/app_theme.dart';
