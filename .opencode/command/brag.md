---
description: Generate an introduction / promo video for the WhiteMountain app (animated HTML/CSS reel rendered to MP4).
agent: build
---

Use the `brag` skill to generate an introduction video for this project app.

Follow the skill's workflow: refresh the copy against `Project_Briefing.md` and
the app source, edit `assets/intro.template.html` if needed, then render with
`node scripts/render.mjs` from `.opencode/skills/brag`.

User arguments: $ARGUMENTS
